# W4 State Graph Test Fixtures v2

This directory contains deterministic test fixtures for W4 local state mirror and incremental routing.

- `manifest.json`: Precise index of all test fixtures in this directory.
- Future fixture files will be introduced incrementally per task card (e.g. `math.json`, `graph.json`, `ingest.json`).
