# 旧计划/已有施工的无损迁移

新活动计划：`ARC-4B-v3.1-20260911-13817f4`；安装在`/root/projects/crypto/arc-chain/docs/plan_v3`，不覆盖`docs/plan_v2`。
旧计划只作历史，不继续发A0–A7/M00–M11新工单。

## 尚未施工

M1按T01–T04建立目标与协调记录。不需要先部署旧版，不需要分别开8个窗口，也不需要用户把48张票逐个贴进去。

## 已经按旧计划开过窗口或有代码

先记录old plan id、当前主脑/子代理、任务、pid/进程启动身份、worktree/branch、dirty变化、账本路径和证据。不要reset/clean/stash覆盖，不删除worktree，不因计划升级就kill用户原服务。

M1停止发旧版新租约，在安全边界等待/协调当前旧writer提交或暂停；既有运行服务与真钱行为的停止需遵守已有授权。根据`migration-map.json`映射旧票至T票：旧A0/A6→M1，A1/A2→M2，A3/A4/A5→M3，A7→M4。

已交付代码不重做：先比较文件/接口/源SHA与本版差异。状态可记`REUSED_PENDING_DELTA_REVIEW`，有本轮证据后才能转ACCEPTED；不能因为任务名字相同自动继承PASS。旧证据仍保留source/candidate哈希，不改日期冒充新跑。

以项目原有instance为主，不新建第二.coord。为兼容本版，M1在协调根设置active_plan_id并记录migration epoch；保留历史任务表和回执。旧epoch迟到结果入历史待审，不进入新合并队列。

## 如果仓库在本次计划之后又更新

本版仍固定`13817f4e027375dd59cc7a202ae641c068525f53`，M1记录新HEAD与差异，不让各窗自动pull。先区分代码与池数据。只改池目录按07登记正常数据差异，不更换代码锁、不重复审核全部工单。确实需要新代码时，M1形成统一代码来源/补丁版本、影响工单和需复测项，M4检查对应范围后再批量切换。

## 冲突裁决

保全当前用户资产和代码优先。缺失的本地路径/实际工具能力由M1调查，不让用户猜技术参数。范围/成本/真实权限变化才集中询问用户，单一接口问题在已批准工程范围内由主脑举证协调。
