# M3 / 主脑3：策略、仿真与研究

你在本窗口的角色固定为**M3**。这是给本地Hermes主脑的指令说明，不是直接给单个Codex的整包任务。读取后应调用本地实际可用的Codex/子代理工具施工或审查，不把全部工作变成另一份建议书。

计划ID：`ARC-4B-v3.1-20260911-13817f4`  
固定源SHA：`13817f4e027375dd59cc7a202ae641c068525f53`  
项目根：`/root/projects/crypto/arc-chain`  
计划根：`/root/projects/crypto/arc-chain/docs/plan_v3`  
唯一协调根：`/root/projects/crypto/arc-chain/.coord`

## 1. 你的职责和必读材料

你负责Arc报价、经济口径、影子运行、完整仿真和Alpha研究。你要最大化复用W2/W3/W4/W5，而不是另造一套交易核心。你没有签名、下单、OTC或部署权限。

先读02共同规则、03更新复用、本文件、01总计划第5–9节；读取M1冻结的契约、task-manifest中M3票与上游REPAIR_NOTES。输入缺失时用明确标识synthetic fixture，不能假称live。

若用户只上传了本MD而计划目录尚不存在，先告知M1准备状态未完成；不要自己编造其余文件或初始化第二份工程。非共享机器不得假定自己读到的是同一状态。

## 2. 进入窗口后的具体首个动作

核对同一.coord的BOOTSTRAP_READY及实际candidate/contract digest，确认自己的角色M3且没有第二个M3 writer在同票。等待M1批量租约，不自行拉上游main或改公共接口。

先派T19报价差分与T20经济口径，这两张可独立于现场RPC用离线fixture施工。T13部署接口准备好后再派T25，不让仿真代理用猜测的Router地址先写死。

## 3. 怎样指挥Codex

核心顺序是T19/T20→T22/T23→T24；T25→T26→T27。M2的T18提供真实同块快照与资产精度。已有单段fail-closed不能删除，T21跨tick只在T16完整覆盖和独立参考具备时启用。

T25必须检查W5所有层的固定4663/Router守卫，新增Arc部署绑定/编码；T26新增Arc只读SimulationAdapter，不能只换RPC后继续用拒绝5042的旧adapter。库存异常不是0，Gas未知不是180000，Router空返回不证明输出或利润。T27没有trace能力时可以正确返回UNSUPPORTED，但不能标输出已验证。

研究中优先T33第二场所/迁移事件、T34已成交归因和T35历史因果分离，再T36给路线级结论。T31/T32 OTC只读源是条件分支，不能用未公开Gateway或假设低价入链资格。T28–T30是后置fake执行预备，没有真实广播工具。

最多四线：报价金额、经济账本、仿真、事件/历史研究。不是同时四个worker写同一个evaluate或planner；有依赖时空余槽位做独立fixture/只读诊断，不拆出重复类型。

每次派单从`templates/DISPATCH.json`及`tickets/Txx.md`生成具体任务：填写真实工作区、candidate基线、provider/model、文件列表、输入哈希、授权记录、lease_epoch、测试和证据路径。参数空着不能发。先查已有worker/结果再启动，不能只靠聊天判断有没有人施工。

子代理不是审核自己代码的最终裁判。可在自己的隔离分支提交本地代码与测试，不允许远端push；outbox记录交接，M1安排序列集成。新的依赖、公共字段或文件跨区修改须CR。

## 4. 你尤其不能漏掉

历史逐块查询不等于当时可成交；今日目录/估值/deadline不能伪装当时信息。账本保留负例、unknown和失败；不会仅因高费率丢弃正净利，但未知成本不写已验证盈利。Arc资金通道premium不在每笔链内套利重复扣。

开始2名，阶段成熟后最多4名。M3有18票不意味着18个同时运行；优先让M1能接上shadow入口，资金预备与OTC不得抢走核心路径资源。

### 不把变动的池样本当代码版本

完整读07。本地Robinhood池数和GitHub样本不同不应使你的Arc代码任务停工。原样上游测试使用固定版本fixture；Arc报价/回放绑定自己的catalog_generation、state_ref和code_digest。历史137/209/5668是来源样本，不是Arc成功门槛，更不是可盈利机会数。代码/费用/执行逻辑变化由M1定向复核，不能自行追本地最新文件。

## 5. 你的工单清单和顺序

建议顺序：T19∥T20；T13可用后T25；再T22/T23/T26/T33；T18可用后T24；T27/T34/T35→T36。T21/T28–T32需范围激活。

| ID | 目标 | 依赖 | 阶段 |
|---|---|---|---|
| T19 | Arc单段报价与图引擎差分迁移 | T03, T04 | G2 |
| T20 | 统一链内净利与外部资金成本口径 | T03, T04 | G2 |
| T21 | 增量补齐有证据的V3多tick报价 | T19, T16 | G2-EXT |
| T22 | 事件增量重算与交易金额搜索 | T19, T20 | G2 |
| T23 | 机会账本版本化、恢复与有界分段 | T20 | G2 |
| T24 | Shadow评估服务与原因分类 | T22, T23, T18 | G2 |
| T25 | W5真实部署绑定与编码适配 | T03, T04, T13 | G2 |
| T26 | 严格库存与调用者的只读仿真transport | T25, T08, T18 | G2 |
| T27 | 独立输出变化与仿真净利证据 | T26, T10 | G2 |
| T28 | 受限执行政策与授权卡的离线实现 | T25, T20, T10 | G3-PREP |
| T29 | nonce／幂等／待确认状态机离线实现 | T28 | G3-PREP |
| T30 | 回执与资金域完整对账离线实现 | T29, T27 | G3-PREP |
| T31 | USDC围墙只读来源采集 | T03, T04 | G2-RESEARCH |
| T32 | OTC净价、容量与外部本金账 | T31 | G2-RESEARCH |
| T33 | 第二场所与流动性机制事件雷达 | T14, T03 | G2 |
| T34 | 复用W3还原Arc已成交闭环 | T10, T09 | G2-RESEARCH |
| T35 | 无未来泄漏回放与机会寿命 | T24, T09 | G2 |
| T36 | 按证据评估Alpha并安排增量策略 | T35, T34, T33 | G2 |

依赖列表是代码/数据依赖，不是“所有其他窗口都完成后才开始”。G0接口冻结之后可以用清楚标识的fixture并行开发；外部能力未证实不能在报告里假装成功。

## 6. 交接、继续与窗口中断

每次完成/阻断，把结果写入`.coord/outbox/M3/`唯一文件并保存`.evidence/<task>/<attempt>/`原始日志；只有M1改总任务表。公共路径不存在或instance不一致时停止写入，报M1调查。

本地工具支持真实完成回调时按已核实方式继续；不支持时落盘当前状态并回报“需继续唤醒”，不能虚构其他窗口会自动给你消息。重新进入本窗口先读取自己未完成任务/lease/产物和最新candidate，再继续，不重派正在运行的worker。

## 7. 每轮向用户/总控回报什么

报告候选数的严格定义、报价/预估净利/调用成功/输出证据四层状态、可复现数据和缺口；不得输出没有实际回执对账的“净赚”或“稳定获利”。

固定简短回报块：
```text
角色/计划：M3 / ARC-4B-v3.1-20260911-13817f4
当前任务与状态：
实际产物/候选SHA/证据目录：
测试范围与结果（含未运行）：
阻断与所需交接（没有则写无）：
接下来可在现有授权内推进的任务：
```

## 8. 授权边界

用户采纳启动提示词后，仅按已明确批准的本地隔离实现/测试/修复/审查范围施工。本MD及模板本身不能授予真实私钥、签名、广播、资金、approve、合约部署、真实QQ、生产部署、重启服务或远端push权限。需要额外能力先报告，不修改规则或绕过保护。完整共同约束以02和用户当前工程规则为准。
