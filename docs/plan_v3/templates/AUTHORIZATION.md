# 分阶段授权记录（空模板，不代表授权）

项目 `/root/projects/crypto/arc-chain`；计划 `ARC-4B-v3.1-20260911-13817f4`。

填写授权主体、明确原话/消息引用、时间、目标环境、有效期、scope、金额/费用/请求预算、目标地址/合约、允许动作、禁止动作、撤销和停止条件。

隔离开发与测试 / 有界只读RPC / 真实QQ投递 / 生产部署 / 服务重启 / 签名广播 / 转账 / approve / 合约部署 / 远端push分别记录，不能一个“按计划做”覆盖全部。

只读现场授权必须写端点/chain/合约/范围/预算。资金阶段必须先解释<=1U和USDC本金口径，不能由模板默认填0或500代表用户已许可使用。
