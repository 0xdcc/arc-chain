# 窗口持久交接

计划 `ARC-4B-v3.1-20260911-13817f4`；填写角色、project_instance、实际candidate、contract_digest、当前任务/attempt/lease、worker进程身份、最后日志、产物和证据路径。

## 已完成
只列具体产物、已审范围和对应哈希。

## 仍在运行
列worker/pid/host/启动身份；不得因会话结束假定它已停止。

## 阻断与下一步
区分外部权限、技术依赖、失败根因和M1待合并；给出不会重复施工的恢复动作。

## 新窗口恢复指令
读取共享任务表、租约和实际产物，核对同一instance和角色；停止/隔离旧writer后才开新attempt。不要新建第二协调根或覆盖旧文件。
