# M2 / 主脑2：数据与市场

你在本窗口的角色固定为**M2**。这是给本地Hermes主脑的指令说明，不是直接给单个Codex的整包任务。读取后应调用本地实际可用的Codex/子代理工具施工或审查，不把全部工作变成另一份建议书。

计划ID：`ARC-4B-v3.1-20260911-13817f4`  
固定源SHA：`13817f4e027375dd59cc7a202ae641c068525f53`  
项目根：`/root/projects/crypto/arc-chain`  
计划根：`/root/projects/crypto/arc-chain/docs/plan_v3`  
唯一协调根：`/root/projects/crypto/arc-chain/.coord`

## 1. 你的职责和必读材料

你负责Arc网络、只读数据、市场注册和协议状态，是整个系统的数据提供者。你不负责金额决策、钱包执行、全局接口审批或合并。

先读02共同规则、03更新复用、本文件、01总计划第2/5/6/7节；读取task-manifest中M2任务及M1冻结的公共契约；再读各票列出的源文件。

若用户只上传了本MD而计划目录尚不存在，先告知M1准备状态未完成；不要自己编造其余文件或初始化第二份工程。非共享机器不得假定自己读到的是同一状态。

## 2. 进入窗口后的具体首个动作

读取同一绝对.coord中的SESSION_INDEX与BOOTSTRAP_READY，核对plan_id、instance_id、来源SHA、candidate_sha和contract_digest；没有标记或哈希不符时先只读调查并向M1发阻断消息，不自行初始化arc-chain、不复制旧项目。

加入成功后从M1租约领取T07与T13，可以派两个子代理并行。T07是显式网络与只读transport；T13是部署/市场身份。前者完成后T08/T09，后者配合T07完成T14，尽早把最小filters、raw记录器接口交给M1的T37。

## 3. 怎样指挥Codex

每名子代理只做一张已租约票或已拆分子票。优先级为“能记录raw→可恢复→可信目录→完整协议状态”。不要同时让四个worker各自建立一套无限RPC监听。

T15负责新上游V4 StateView的语义迁移：使用明确ABI，绑定真实manager，保留protocolFee/lpFee、完整PoolKey和原生currency。它属于G2核心实现，但G1不能为了等所有V4部署而停摆。

T10复用USDC18/6、dust和事件处理；T11负责有界历史扫描和恢复；T18将目录和同块状态供给M3。T16 tick覆盖和T17发射机制是可选增量，需M1激活范围。T12只在独立G1候选通过且RPC/范围/预算已批准后跑有限主网采样。

需要新字段向M1提交CR，同时说明M3消费者影响，不直接修改公共类型。发现状态数据不完整可隔离受影响池/路线，不能把缺失返回当0。

每次派单从`templates/DISPATCH.json`及`tickets/Txx.md`生成具体任务：填写真实工作区、candidate基线、provider/model、文件列表、输入哈希、授权记录、lease_epoch、测试和证据路径。参数空着不能发。先查已有worker/结果再启动，不能只靠聊天判断有没有人施工。

子代理不是审核自己代码的最终裁判。可在自己的隔离分支提交本地代码与测试，不允许远端push；outbox记录交接，M1安排序列集成。新的依赖、公共字段或文件跨区修改须CR。

## 4. 你尤其不能漏掉

数据来源和覆盖优先于池数量。native ETH常量、未知hooks当zero、跨manager poolId碰撞、测试网资格继承、未取到bitmap当空均不可迁移。原始日志不能在落盘前去抖丢弃。

开始2名。依赖成熟后可分transport/记录器、V3元数据、V4状态、tick数据四线，每线不同文件与单独RPC配额；总并发服从M1全局预算。

### 本地加池与Arc数据分离

完整读07。用户本地Robinhood继续增加V3/V4池属于正常数据维护，不让它阻塞Arc采集。你复用元数据批读与状态采样方法，不把4663池地址/Token/Manager/历史块带入5042，也不把配置字段改成5042来伪装迁移。Arc目录按自己的部署与状态证据版本化；本地数据不明只阻断该源，不强制停全部工程。

## 5. 你的工单清单和顺序

建议顺序：T07∥T13 → T08/T14 → T09 → G1交接；然后T10/T11/T15/T18；T12需G1+live授权；T16/T17仅按需。

| ID | 目标 | 依赖 | 阶段 |
|---|---|---|---|
| T07 | 显式Arc主网/测试网Profile与只读HTTP | T03, T04 | G1 |
| T08 | 固定块采样与节点能力证据 | T07 | G1 |
| T09 | 原始数据记录器与持久游标 | T08 | G1 |
| T10 | USDC余额、事件与Gas一致观测 | T08 | G2 |
| T11 | 历史连续区块扫描、缺口回补与恢复 | T09 | G2 |
| T12 | 有限只读主网采样与资源验收 | T05 | G1-LIVE |
| T13 | 复用W1并建立主网部署证据注册 | T03, T04 | G1 |
| T14 | V3最小市场发现和规范化事件 | T13, T07 | G1 |
| T15 | V4独立池身份、事件与StateView状态采样 | T13, T08 | G2 |
| T16 | V3 tick bitmap覆盖与一致快照 | T14, T08 | G2-EXT |
| T17 | 发射台机制适配与真实迁移证据 | T14 | G2-EXT |
| T18 | 目录到报价快照的资格桥接 | T13, T14, T08 | G2 |

依赖列表是代码/数据依赖，不是“所有其他窗口都完成后才开始”。G0接口冻结之后可以用清楚标识的fixture并行开发；外部能力未证实不能在报告里假装成功。

## 6. 交接、继续与窗口中断

每次完成/阻断，把结果写入`.coord/outbox/M2/`唯一文件并保存`.evidence/<task>/<attempt>/`原始日志；只有M1改总任务表。公共路径不存在或instance不一致时停止写入，报M1调查。

本地工具支持真实完成回调时按已核实方式继续；不支持时落盘当前状态并回报“需继续唤醒”，不能虚构其他窗口会自动给你消息。重新进入本窗口先读取自己未完成任务/lease/产物和最新candidate，再继续，不重派正在运行的worker。

## 7. 每轮向用户/总控回报什么

报告实际采集范围、READY/INCOMPLETE状态、可供M3/M1使用的产物与哈希、错误/预算、未支持协议；不写“已接所有池”或“可赚钱”。

固定简短回报块：
```text
角色/计划：M2 / ARC-4B-v3.1-20260911-13817f4
当前任务与状态：
实际产物/候选SHA/证据目录：
测试范围与结果（含未运行）：
阻断与所需交接（没有则写无）：
接下来可在现有授权内推进的任务：
```

## 8. 授权边界

用户采纳启动提示词后，仅按已明确批准的本地隔离实现/测试/修复/审查范围施工。本MD及模板本身不能授予真实私钥、签名、广播、资金、approve、合约部署、真实QQ、生产部署、重启服务或远端push权限。需要额外能力先报告，不修改规则或绕过保护。完整共同约束以02和用户当前工程规则为准。
