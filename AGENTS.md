# AGENTS.md — Arc-Chain 工程约定 (v3.1)

> 任何 AI（Codex / Hermes / 子代理）修改本仓库前**必须先读本文件**。

## 1. 项目定位与四主脑架构
Arc 独立全链套利与只读研究引擎（目标链：Arc 主网 5042，只读研究）。
固定四主脑分工：
- **M1**：总控与集成（唯一合并者、公共契约、CLI入口与报告）
- **M2**：数据与市场（RPC、USDC双视图、原始采集、V3/V4目录与状态）
- **M3**：策略/仿真/研究（两三跳报价、成本账本、只读模拟、历史因果分析）
- **M4**：独立审查（原测试义务核对、F01-F07迁移、独立E2E与门禁审核）

## 2. 🔴 不可协商的资金与操作安全红线
1. **零真实资金与网络操作**：本工程为只读与离线研究系统。严禁发起真实交易、转账、approve、合约部署、生产服务重启或远端 git push。
2. **私钥与凭据物理封印**：严禁硬编码私钥、助记词或将凭据写入代码、日志或测试用例中。
3. **滑点与保底刚性门禁**：滑点禁止为 0。最低输出必须满足保底公式 `output_floor >= amount_in + ceil(gas_atoms) + 1 atom`。
4. **单笔上限 ≤ 500 USD**：超限一律 fail-closed 抛错。
5. **Dry-run 默认开启**：系统默认 `ARMED = False`，仅支持纯只读仿真与模拟。
6. **三次失败熔断**：RPC 连续失败 3 次立即熔断停机，严禁死循环重试或自动重启绕过。

## 3. 跨主脑协作与租约规则
- **唯一协调根**：`/root/projects/crypto/arc-chain/.coord`
- **严格租约**：每个子代理只能写入租约中明确声明的文件白名单，新建 `__init__.py` 或跨包修改须向 M1 提交变更请求。
- **独立工作区**：每个实际代码 writer 使用独立 worktree，禁止共享 index、TMPDIR 或运行时账本。
- **代码与池数据解耦**：遵循 `07_本地池目录变化处理规则.md`，Robinhood 池清单不导入 Arc 注册表。

## 4. 门禁自检标准
- Python 3.12，使用 `./venv/bin/python`。
- 测试运行：`./venv/bin/python -m pytest tests/`
- 代码规范：`ruff check` 零错误，类型检查保持严格。
